services:
  server:
    image: server
    env_file: .env
    build:
      context: server
    ports:
      - 3001:3001

  bot1:
    image: bot1
    build:
      context: "${BOT_1_PATH}"
    depends_on:
      - server

  bot2:
    image: bot2
    build:
      context: "${BOT_1_PATH}"
    depends_on:
      - server
 